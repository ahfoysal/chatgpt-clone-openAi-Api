/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["joshbersin.com", "oaidalleapiprodscus.blob.core.windows.net"],
  },
};

module.exports = nextConfig;
